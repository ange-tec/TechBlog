{% extends 'base.html.twig' %}

{% block title %}Accueil - TechBlog{% endblock %}

{% block body %}
    <!-- Section Hero -->
    <section class="bg-gradient-to-r from-blue-600 to-indigo-700 text-white py-24">
        <div class="max-w-6xl mx-auto text-center px-6">
            <h1 class="text-5xl font-extrabold mb-4">Bienvenue sur <span class="text-yellow-300">TechBlog</span></h1>
            <p class="text-lg text-blue-100 max-w-2xl mx-auto mb-8">
                Découvrez les derniers articles, tutos et news sur le développement web, la tech et bien plus encore.
            </p>
            {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="{{ path('app_login') }}" class="bg-yellow-400 hover:bg-yellow-500 text-blue-900 font-semibold px-6 py-3 rounded-lg shadow">
                    Connexion
                </a>
                <a href="{{ path('app_register') }}" class="ml-4 border border-yellow-300 hover:bg-yellow-400 hover:text-blue-900 px-6 py-3 rounded-lg shadow">
                    Créer un compte
                </a>
            {% else %}
                <a href="{{ path('app_articles') }}" class="bg-white hover:bg-gray-100 text-blue-700 font-semibold px-6 py-3 rounded-lg shadow">
                    Voir les articles
                </a>
            {% endif %}
        </div>
    </section>

    <!-- Section À propos -->
    <section class="py-16 bg-gray-50">
        <div class="max-w-5xl mx-auto text-center px-6">
            <h2 class="text-3xl font-bold text-gray-800 mb-6">Pourquoi TechBlog ?</h2>
            <p class="text-gray-600 leading-relaxed max-w-3xl mx-auto mb-10">
                TechBlog est une plateforme collaborative dédiée aux passionnés de technologie et de développement.
                Notre mission est de partager des connaissances, des bonnes pratiques et des actualités du monde numérique.
            </p>

            <div class="grid gap-8 md:grid-cols-3">
                <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition">
                    <div class="text-blue-600 text-4xl mb-4"></div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">Articles inspirants</h3>
                    <p class="text-gray-600">Explorez des articles clairs, pratiques et rédigés par des développeurs passionnés.</p>
                </div>

                <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition">
                    <div class="text-blue-600 text-4xl mb-4"></div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">Tutoriels pratiques</h3>
                    <p class="text-gray-600">Apprenez à maîtriser Symfony, PHP, JavaScript, et bien d’autres technologies modernes.</p>
                </div>

                <div class="bg-white p-6 rounded-lg shadow hover:shadow-lg transition">
                    <div class="text-blue-600 text-4xl mb-4"></div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">Communauté active</h3>
                    <p class="text-gray-600">Participez à une communauté de développeurs et partagez vos connaissances.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Section Derniers articles -->
    <section class="py-16 bg-white">
        <div class="max-w-6xl mx-auto px-6">
            <h2 class="text-3xl font-bold text-gray-800 mb-10 text-center">Derniers articles</h2>

            <div class="grid gap-8 md:grid-cols-3">
                {% for article in articles|slice(0, 3) %}
                    <div class="bg-gray-50 rounded-lg shadow hover:shadow-lg transition overflow-hidden">
                        {% if article.image %}
                            <img src="{{ asset('uploads/' ~ article.image) }}" alt="Image article" class="w-full h-48 object-cover">
                        {% endif %}
                        <div class="p-6">
                            <h3 class="text-xl font-semibold text-gray-800 mb-2">{{ article.title }}</h3>
                            <p class="text-gray-600 text-sm mb-4">{{ article.summary|slice(0, 100) ~ '...' }}</p>
                            <a href="{{ path('app_article', { id: article.id }) }}" class="text-blue-600 font-medium hover:underline">Lire l’article →</a>
                        </div>
                    </div>
                {% else %}
                    <p class="text-center text-gray-500 col-span-3">Aucun article pour le moment.</p>
                {% endfor %}
            </div>
        </div>
    </section>

    <!-- Section Footer -->
    <footer class="bg-gray-800 text-gray-300 text-center py-8 mt-16">
        <p>&copy; {{ "now"|date("Y") }} <span class="text-white font-semibold">TechBlog</span> — Tous droits réservés.</p>
        <p class="text-sm mt-2">Développé avec ❤️ en Symfony & Tailwind CSS</p>
    </footer>

{% endblock %}
